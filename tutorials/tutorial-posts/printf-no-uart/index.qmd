---
title: 'Using `printf` for MCU debugging'
categories: [MCU]
---

In Lab 6 you will learn how to use the UART peripheral to send debugging logs to your laptop from the MCU.

The MCU can also print to a specialized debug channel using `ITM_SendChar` and `printf`:
```c
// Necessary includes for printf to work
#include <stdio.h>
#include "stm32l432xx.h"

// Function used by printf to send characters to the laptop
int _write(int file, char *ptr, int len) {
  int i = 0;
  for (i = 0; i < len; i++) {
    ITM_SendChar((*ptr++));
  }
  return len;
}

int main(void) {
  int i;

  for (i = 0; i < 100; i++) {
      // printf
    printf("Hello World %d!\n", i);
  }
  do {
    i++;
  } while (1);
}
```

Note that this only works in the debug mode, and the logs appear here:

TODO: insert image here from running this code on lab computers

If you want to include floating point values in your prints, you need to enable Printf Floating Point Support under Project Options -> Linker -> Printf/Scanf.